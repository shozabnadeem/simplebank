version: 1
packages:
  - path: "./db/sqlc"
    name: "db"
    schema: "./db/migration"
    queries: "./db/query/"
    engine: "postgresql"
    emit_json_tags: true
    emit_interface: false
    emit_exact_table_names: false
    emit_prepared_queries: true